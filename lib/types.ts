export type Status = "idle" | "loading" | "success" | "error";

export type Description = {
  language: string;
  description: string;
};

export type Language = {
    value: string;
    label: string;
  };
  
