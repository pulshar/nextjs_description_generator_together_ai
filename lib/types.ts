export type Status = "idle" | "loading" | "success";

export type Description = {
  language: string;
  description: string;
};

export type Language = {
    value: string;
    label: string;
  };
  
